import * as React from 'react';
import {View, Text, Button} from 'react-native';

const SessionsScreen = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>This is Sessions</Text>
    </View>
  );
};
export default SessionsScreen;
